木上の距離を求めるデータ構造．

非空の連結な辺重み付き木を管理し，LCA を用いて 2 頂点間の距離を計算する．辺は `to` と `weight` を持つものとする．

- `TreeDistance<T>(g)`：木 `g` を頂点 $0$ から前処理する．
- `size()`：頂点数を返す．
- `dist(u, v)`：頂点 $u,v$ 間の距離を返す．

前処理は $O(N\log N)$ 時間，距離クエリは $O(1)$ 時間．空間計算量は $O(N\log N)$．
